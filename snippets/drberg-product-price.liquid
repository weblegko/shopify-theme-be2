{% comment %}
  Renders a product's price for the product page (regular, sale)
  Usage: {% render 'drberg-product-price', product: product %}
{% endcomment %}
{%- liquid
  if use_variant
    assign target = product.selected_or_first_available_variant
  elsif placeholder
    assign target = null
  else
    assign target = product
  endif

  assign compare_at_price = target.compare_at_price
  assign price = target.price | default: 1999
  assign available = target.available | default: false

  assign money_price = price | money
  assign money_compare = compare_at_price | money

  assign show_from = false
  if target == product and product.price_varies
    assign show_from = true
  endif

  assign is_on_sale = false
  if compare_at_price > price
    assign is_on_sale = true
    assign savings = compare_at_price | minus: price
    assign savings_percent = savings | times: 100 | divided_by: compare_at_price
  endif
-%}

{%- unless target == null and placeholder == null -%}
  <div class="drberg-product-price{% if price_class %} {{ price_class }}{% endif %}">

    {% comment %} Левая часть: цена и старая цена  {% endcomment %}
    <div class="drberg-product-price__inner">
      {%- if is_on_sale -%}
        <span class="drberg-product-price__current">
          {%- if show_from -%}<span>From </span>{%- endif -%}        
            {{ money_price }}          
        </span>

        <span class="drberg-product-price__vat">
          incl VAT <span class="drberg-product-price__vat-old">{{ money_compare }}</span>
        </span>
      {%- else -%}
        <span class="drberg-product-price__current">
          {%- if show_from -%}<span>From </span>{%- endif -%}          
            {{ money_price }}          
        </span>
      {%- endif -%}
    </div>

     {% comment %} Правая часть: процент скидки  {% endcomment %}
    {%- if is_on_sale and savings_percent > 0 -%}
      <span class="drberg-product-price__badge">
        save {{ savings_percent }}%
      </span>
    {%- endif -%}

  </div>
{% endunless %}